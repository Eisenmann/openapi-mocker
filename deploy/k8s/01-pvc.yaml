apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openapi-mocker-data
  namespace: openapi-mocker
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName: standard   # uncomment and set your own StorageClass if needed
